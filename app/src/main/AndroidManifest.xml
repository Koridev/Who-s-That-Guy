<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.korigan.whosthatguy" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WhosThatGuyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/WhosThatGuyTheme" >
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateHidden|adjustPan" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.ActorDetailActivity"
            android:label=""
            android:launchMode="singleTop">
        </activity>
        <activity
            android:name=".ui.activity.AboutActivity"
            android:label="@string/title_activity_about" >
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="1d2663d601ddf02367fac4acff989cf1eea05f13" />

        <activity
            android:name=".ui.activity.MovieDetailActivity"
            android:label="" >
        </activity>
    </application>

</manifest>
